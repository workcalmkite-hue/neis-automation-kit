# 나이스 자동화 키트 — Claude Code 작업 규칙

이 저장소는 **교사가 본인 나이스 업무를 자동화**하는 도구다.
사용자는 대부분 **개발자가 아니고, VS Code·터미널을 처음 쓰는 선생님**이다.

---

## 0. 이 사용자에게 말하는 법

- **한국어로** 답한다.
- **"이 정도는 아시겠지"로 건너뛰지 않는다.** 폴더 여는 법, 터미널 여는 법,
  저장(●이 남아 있으면 저장 안 된 것), 껐다 켜기까지 적는다.
- **"파일을 열어 고치세요"는 지시가 아니다.** 어디를 눌러 여는지, 고친 뒤
  무엇을 해야 반영되는지까지 적는다.
- 에러 화면을 만나면 **"잘못하신 게 아닙니다"를 먼저** 말한다. 처음 쓰는 분은
  빨간 글씨를 보면 처음부터 다시 깔려고 한다.
- 주소를 알려줄 때는 **클릭되는 마크다운 링크**로 준다. 벌거벗은 URL을 코드블럭에 넣지 않는다.
  단, **그대로 복사해서 붙여넣을 내용**(메시지·명령어)은 코드블럭에 넣는다.

---

## 1. 절대 하면 안 되는 것

### 1-1. 개인정보를 저장소에 남기지 않는다

**이 저장소에는 실명·학번·연락처·인증서 이름·학교명이 한 글자도 들어가면 안 된다.**

- 학생 이름·번호·출결 사유가 든 파일을 저장소 안에 만들지 않는다
  (`.csv`, `.xlsx`, `.png`, `.log` 는 `.gitignore`로 막혀 있지만, **믿지 말고 만들지 마라**)
- 디버깅용 스크린샷·로그는 **저장소 밖**(예: `%TEMP%`)에 만든다
- 커밋 전에는 항상 아래를 돌린다:
  ```bash
  git config core.quotepath false
  git status -uall
  git ls-files | grep -Ei "token|credential|secret|\.env|출결|명렬|학생|명단|\.csv|\.xlsx"
  ```
  ⚠️ `core.quotepath false` 를 빼먹으면 git이 한글 경로를 이스케이프해서 출력하므로
  **한글 파일명 grep이 영원히 0건**으로 나온다.
- `.gitignore`에 넣는 것만으로는 부족하다. **이미 추적 중이면 `git rm --cached` 까지** 해야 빠진다.

### 1-2. 자격증명을 코드에 넣지 않는다

- 인증서 비밀번호는 **keyring에서만** 읽는다. 대화에 평문으로 적지 않는다.
- 구글 `client_id`/`client_secret`은 `oauth_client.json`(gitignore)에만 둔다.
- 값이 필요하면 코드가 읽게 하고, **화면에 출력하지 않는다.**

### 1-3. 사용자가 시키지 않은 커밋·푸시를 하지 않는다

파일을 고친 뒤 알아서 커밋하지 않는다. "커밋해줘 / 푸시해줘"라고 **말할 때만** 한다.

---

## 2. 설정값은 코드에 하드코딩하지 않는다

학교·지역·개인마다 다른 값은 전부 `%LOCALAPPDATA%\neis-automation\teacher_config.json` 에 있다.
새 기능을 만들 때 아래를 **코드에 박지 말고 설정에서 읽는다.**

| 설정 키 | 무엇 |
|---|---|
| `neis_url` | 시도교육청별 나이스 주소 |
| `grade` / `class` | 담임 학급 |
| `cert_name` | 공동인증서 이름 (비밀번호는 keyring) |
| `contact` | 복무 신청 연락처 |
| `approval_line` | 나이스 개인결재선 프리셋 이름 |
| `holiday_sheet_id` / `holiday_sheet_range` | 휴업일 시트 (선택) |
| `last_period` | 하루 최대 교시 수 |
| `calendars` | 분류별 구글 캘린더 ID |

읽는 법:
```python
import teacher_config
cfg = teacher_config.load_config()   # DEFAULTS가 병합된 dict를 준다
```

---

## 3. 파일 구조

```
main.py              학생 출결 자동 입력 (구글 캘린더 → 나이스)
main_bokmu.py        본인 복무(조퇴·외출·지각) 신청·상신
verify_attendance.py 캘린더 vs 나이스 대조 (읽기 전용)
setup_wizard.py      최초 설정 마법사
fix_cert.py          인증서 이름·비밀번호만 다시 설정
teacher_config.py    설정 저장/로드
app.py               exe 배포용 진입점 (설정 없으면 마법사부터)
mcp_server.py        Claude Code용 MCP 서버 (캘린더 생성·출결 입력)
neis_session.py      나이스에 로그인해 둔 크롬을 열어 둔다 (출장·41조처럼 스크립트 없는 화면용). --check 는 점검만
neis_cdp.py          열어 둔 크롬에 붙어 스니펫 파일을 실행 (브라우저 도구로는 나이스 로그인이 안 된다)
.mcp.json            MCP 연결 정보 — 폴더를 열면 자동으로 읽힌다
.claude/skills/      Claude Code 스킬 (출결·복무·출장·41조연수·캘린더 입력)
docs/                선생님용 안내 문서
```

---

## 3-1. MCP 서버를 고칠 때

`mcp_server.py`는 설정 마법사가 만든 구글 토큰(`teacher_config.TOKEN_FILE`)을 그대로 쓴다.
별도 인증을 넣지 말 것.

- **구글 공식 캘린더 MCP에는 캘린더 생성 도구가 없다** (도구 9개 전부 일정용, 2026-08-30 실측).
  그래서 이 서버가 필요하다 — "커넥터로 대체하자"는 제안을 하지 말 것.
- 도구를 추가하면 **stdio 핸드셰이크로 실제 노출되는지 확인**한다.
  함수가 도는 것과 MCP 도구로 잡히는 것은 별개다.
- ⚠️ **`.mcp.json`의 `command`에 `./`를 빼면 윈도우에서 실행 파일을 못 찾는다** (2026-08-30 실측).
  윈도우는 **역슬래시가 있거나 `.`으로 시작할 때만** 경로로 인정한다. 슬래시만 있으면 "이름"으로
  보고 **현재 폴더가 아니라 `PATH`만** 뒤진다 → 못 찾음.
  `.venv/Scripts/python.exe` ❌ / `./.venv/Scripts/python.exe` ✅ / `.venv\Scripts\python.exe` ✅ / 절대경로 ✅
  (구분자가 아예 없으면 현재 폴더에 파일이 실제로 있어도 실패한다 — 확인함)
  **틀려도 오류가 안 뜨고 `/mcp` 목록에서 조용히 빠진다.** 그래서 재시작 문제로 오진하기 쉽다.
- MCP 목록은 **클라이언트가 시작할 때 한 번만** 읽힌다. 고친 뒤에는 VS Code를 껐다 켜야 반영된다.

## 4. 나이스 화면을 다룰 때 (실측으로 확인된 것)

나이스는 일반 웹페이지가 아니라 **클립소프트(cl-) 계열 UI**다. 아래는 실제로 겪은 함정이다.

- **커스텀 콤보박스는 `select_option`이 안 먹는다.** 클릭해서 열고 옵션을 클릭해야 한다.
  목록이 **가상 스크롤**이라 화면에 없는 값은 스크롤하거나 `ArrowUp/Down`으로 찾아야 한다.
- **날짜 입력칸에 `fill()`을 쓰면 마스크가 깨진다.** 반드시 **달력 아이콘 → 날짜 버튼** 클릭.
- **연락처 칸도 마스크다.** `Ctrl+A` → `Delete` → 한 글자씩 천천히 입력한다.
- **사이드바 클릭이 오버레이에 막힌다.** `a[title="..."]` 로 잡고 좌표 클릭(`position:{x:20,y:8}`)한다.
  메뉴의 접근성 텍스트는 `"출결관리 2단계"` 형태라 정확히 일치 검색은 0건이 나온다.
- **숨은 탭에 같은 이름의 버튼이 있다.** `:visible` 로 한정하고 짧은 timeout을 준다.
  안 그러면 안 보이는 버튼을 눌러 엉뚱하게 동작하거나 30초를 기다린다.
- **전체 DOM 순회 금지.** `document.querySelectorAll('*')` 같은 걸 돌리면 렌더러가 죽어
  페이지가 `about:blank`가 되고 세션이 끊긴다. 확인은 `aria-label` 셀렉터로 좁혀서 한다.
- **전역 "닫기" 버튼 클릭 금지.** 브라우저 창 자체가 닫힌 적이 있다. 다이얼로그 안으로 스코프를 좁힌다.
- 로그인 직후 **공지 팝업**이 메뉴를 가릴 수 있다. 팝업 닫기 + 재시도로 감싼다.

---

## 5. 검증 습관

- **"됐다"고 말하기 전에 실제로 확인한다.** 파일을 열어보고, 명령을 돌려보고,
  화면 값을 읽어본다. 확인 못 했으면 **"확인 안 해봤다"고 그대로 말한다.**
- 근거를 같이 댄다: "몇 건 중 몇 건", "어느 파일 몇 번째 줄", "어떤 명령의 어떤 출력".
- 나이스에 무언가를 입력·상신한 뒤에는 **목록 화면을 다시 읽어** 의도한 값이 들어갔는지 확인하고,
  그 값을 사용자에게 그대로 보고한다.
- 날짜·요일은 **직접 계산하지 말고** 코드로 확인한다.

---

## 6. 정책 리스크 (항상 인지할 것)

이 도구는 **정부 시스템(나이스)을 브라우저 자동화로 조작**한다.
코드에 자동화 탐지 회피(`navigator.webdriver` 은닉, `--disable-blink-features=AutomationControlled`)가
들어 있고, 이는 학교·교육청 지침에 어긋날 소지가 있다.

- 사용자가 **동료에게 배포/공유**하려 하면, 이 리스크를 한 번 짚어준다.
- **남의 계정·인증서로 돌리는 기능은 만들지 않는다.**
- 대량 조회·크롤링처럼 **본인 업무 범위를 넘는 기능**은 제안하지 않는다.
